{% extends 'base.html.twig' %}

{% block title %}FormEvent{% endblock %}

{% block body %}
    <h1>FormEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_event.id }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ form_event.mail }}</td>
            </tr>
            <tr>
                <th>SerialNumber</th>
                <td>{{ form_event.SerialNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_event_index') }}">back to list</a>

    <a href="{{ path('app_form_event_edit', {'id': form_event.id}) }}">edit</a>

    {{ include('form_event/_delete_form.html.twig') }}
{% endblock %}
